<Window x:Class="WpfTestData.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        xmlns:local="clr-namespace:WpfTestData"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.CommandBindings>
        <CommandBinding Command="{x:Static local:MyCommand.CheckControlsCommand}"
            Executed="DrawTestData"
            CanExecute="CheckTestData">
        </CommandBinding>

    </Window.CommandBindings>
    <Grid>
        <lvc:CartesianChart x:Name="TestDataGraph" Series="{Binding Series}" LegendLocation="Right" Background="#FFE3F3E8" Margin="85,139,123,0">

        </lvc:CartesianChart>
        <TextBox TextWrapping="Wrap" Name="LeftTestData" Text="" Width="120" Margin="85,75,595,317"/>
        <TextBox TextWrapping="Wrap" Name="RightTestData" Text="" Width="120" Margin="240,75,440,317"/>
        <Button Content="Draw" HorizontalAlignment="Left" Margin="400,75,0,0" VerticalAlignment="Top" Height="42" Width="117" Command="{x:Static local:MyCommand.CheckControlsCommand}"/>

    </Grid>
</Window>
